ll tree[1000001] = { 0 };
int n;

void update(int i, ll num) {
	while (i <= n) {
		tree[i] += num;
		i += (i & -i);
	}
}

ll find(int i) {
	ll sum = 0;
	while (i > 0) {
		sum += tree[i];
		i -= (i & -i);
	}
	return sum;
}