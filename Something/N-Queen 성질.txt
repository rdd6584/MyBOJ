모든 퀸을 그리드하게 배치했을 때.
	// case1
	for (; i * 2 + 1 < n; i++){
		mat[i][i * 2 + 1] = 1;

	for (int j = 0; j < n; j++)
		mat[i + j][j * 2] = 1;
	}
	// case2
	for (; i * 2 < n; i++){
		mat[i][i * 2] = 1;

	for (int j = 0; j < n; j++)
		mat[i + j][j * 2 + 1] = 1;
	}

Fail의 경우
가설 1 : 결과는 구성된 소수에 관련이 있다.

1. 6의 배수 - 0
2. 3의 배수 - 2
3. 2의 배수 - 1
4. 그 외의 모든 수 - 0
해당 숫자만큼의 테스트 케이스를 통과하지 못했습니다.